# Cortex PRD

## Purpose

Cortex is Beluna's deliberative cognition boundary.

Boundary shape:
`[senses, physical_state(act_descriptor_catalog + proprioception), cognition_state(goal-forest, l1-memory)] -> acts, cognition_state`

## Requirements

- Cortex is a pure boundary function with no direct side effects.
- Cortex does not persist state by itself; persistence belongs to Continuity.
- Cortex accepts empty `senses` on interval ticks.
- Pipeline is fixed:
  - `raw-input -> input-helpers -> <input-ir> -> primary -> <output-ir> -> output-helpers -> real-output`
- Cortex runtime is tick-driven only (senses are buffered; sense arrival does not trigger immediate cycles).
- Primary runs one AI Gateway thread turn per admitted tick cycle.
- If tool-calls are returned on tick `N`, their tool-result messages are injected into tick `N+1` turn together with tick `N+1` input payload.
- Internal tool calls are Internal Cognitive Actions and are distinct from Somatic Act outputs.
- Input IR first-level XML sections:
  - `<somatic-senses>`
  - `<proprioception>`
  - `<somatic-act-descriptor-catalog>`
  - `<goal-forest>`
  - `<focal-awareness>`
- Primary output text does not carry somatic-acts/wait contracts; act emission and wait control are tool-call-native.
- Goal model is `goal-forest` (mutable forest in cognition state) with node shape:
  - `status` (free text)
  - `weight` (`0..1`)
  - `id` (free text, commonly kebab-case short phrase)
  - `summary` (free text)
  - `children` (`GoalNode[]`)
- Goal instincts are consolidated in Primary system prompt (not persisted as root partition field).
- `<goal-forest>` input section is deterministic ASCII-art (`+--`, `|--`) text.
- Goal updates are performed via internal tool `patch-goal-forest`; Output IR has no goal patch section.
- `patch-goal-forest` tool argument contract is hard-coded in `/Users/lanzhijiang/Development/Beluna/core/src/cortex/runtime/primary.rs`.
- Goal weight normalization is removed; incoming weight must already be in `[0,1]`.
- L1 memory model is ordered `string[]`.
- `<somatic-senses>` section entries use process-lifetime monotonic sense ids with explicit `endpoint_id` and `sense_id`.
- `<proprioception>` is deterministic natural-language rendering of `PhysicalState.proprioception`.
- Sense helper behavior:
  - render deterministic line format:
    - `- <monotonic-id>. endpoint_id=<endpoint_id>, sense_id=<sense_id>, weight=<weight>[, truncated_ratio=<0..1 if truncated>]; payload="<payload-truncated-if-needed>"`
- Primary internal tools:
  - `expand-senses(tasks[])` where each task has `sense_id` and optional `use_subagent_and_instruction_is` (mixed raw/sub-agent in one call)
  - `patch-goal-forest(patch_instructions)` where helper returns full replacement `GoalNode[]`
- Output helper JSON contracts are direct arrays (no wrapper envelope fields).
- Output helpers consume section-local context only; full Output IR text is not required.
- `act_instance_id` is generated by deterministic runtime code path (UUIDv7 generator), not by LLM.
- `wait_for_sense > 0` is valid only when the target act carries non-empty `might_emit_sense_ids`.
- Wait-for-sense execution uses tick-skip gating by correlated buffered senses; it does not mutate afferent deferral rules.
- Failure policy:
  - primary failure/timeout: fail-closed noop (`acts=[]`, cognition unchanged)
  - helper failure/timeout: deterministic fallback section or empty output

## Out of Scope

- Direct world execution.
- Queue ownership and tick scheduling.
- Ledger settlement and reservation logic.
